export interface IPost { authorId: number, content: string, createdAt: string, id: number, images: [], status: string, title: string, updatedAt: string }
export interface IUserInfo { id: number, role: string, email: string }
